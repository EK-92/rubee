<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to game.name, game%>
      , <%= game.description %>, <%= timelessness(game) %>
    </li>
  <% end %>
</ul>

<%= link_to "add new", new_game_path %>